import { jobSchema } from "./job";
import { sectorSchema } from "./sector";
import { testimonialSchema } from "./testimonial";

export const schemaTypes = [jobSchema, sectorSchema, testimonialSchema];
